version: "2"
services:
  mongo:
    image: mongo
    volumes:
      - /data/mongodb/db:/data/db
    ports:
      - "27017:27017"
  web:
    build: .
    ports:
      - "8080:3001"
    links:
      - mongo